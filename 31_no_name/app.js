"use strict";
let userNames = ['admin', 'arya', 'stark', 'peter', 'groot'];
if (userNames.length === 0) {
    console.log('we need to find some users!');
}
else {
    userNames = [];
    console.log('all username has been removed. ' + userNames.length);
}
